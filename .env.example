# Port for the webhook relay server (default: 443)
PORT=443

# Shared secret — CloudFront/Cloudflare sends this in X-Webhook-Secret header
# Generate: openssl rand -hex 32
WEBHOOK_SECRET=

# GitHub webhook secret (configured in GitHub repo webhook settings)
# Generate: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=

# TLS — inline PEM values (multiline)
# If both are set, the server starts with HTTPS. Otherwise plain HTTP.
# Paste the full PEM content. Use \n for newlines or use multiline with quotes:
#
# TLS_CERT="-----BEGIN CERTIFICATE-----
# MIIExample...
# -----END CERTIFICATE-----"
#
# TLS_KEY="-----BEGIN PRIVATE KEY-----
# MIIExample...
# -----END PRIVATE KEY-----"
#
TLS_CERT=
TLS_KEY=

# Optional: CA certificate (e.g. Cloudflare Origin CA root)
TLS_CA=
