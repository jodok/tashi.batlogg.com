# Port for the webhook relay server
PORT=3456

# Shared secret — CloudFront sends this in X-Webhook-Secret header
# Generate: openssl rand -hex 32
WEBHOOK_SECRET=

# GitHub webhook secret (configured in GitHub repo webhook settings)
# Generate: openssl rand -hex 32
GITHUB_WEBHOOK_SECRET=
